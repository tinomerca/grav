extends@: default

form:
  fields:
    tabs:
      fields:
        advanced:
          fields:
            columns:
              fields:
                column1:
                   fields:
                     header.body_classes:
                       markdown: true
                       description: 'Available classes in Canvas (space separated):<br />`stretched`, `header-animated`, `header-dark`, `header-transparent`, `sticky-footer`'